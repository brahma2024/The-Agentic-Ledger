# =============================================================================
# The Agentic Ledger - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Required
# -----------------------------------------------------------------------------

# OpenAI API key (used for GPT-4o, TTS, and embeddings)
OPENAI_API_KEY=sk-your-key-here

# -----------------------------------------------------------------------------
# RSS Feed Bundles (Google Alerts → arXiv category mapping)
# -----------------------------------------------------------------------------
# JSON array of feed bundles. Each bundle links RSS feeds to arXiv categories.
# Run `python -m src.main --lexicon` to generate Google Alerts queries.
#
# Example:
# RSS_FEED_BUNDLES='[
#   {
#     "name": "AI Systems",
#     "feed_urls": ["https://www.google.com/alerts/feeds/..."],
#     "arxiv_codes": ["cs.AI", "cs.LG"],
#     "priority": 10,
#     "enabled": true
#   },
#   {
#     "name": "Cybersecurity",
#     "feed_urls": ["https://www.google.com/alerts/feeds/..."],
#     "arxiv_codes": ["cs.CR"],
#     "priority": 8,
#     "enabled": true
#   }
# ]'
RSS_FEED_BUNDLES=

# -----------------------------------------------------------------------------
# LLM & TTS Models
# -----------------------------------------------------------------------------
LLM_MODEL=gpt-4o
TTS_MODEL=gpt-4o-mini-tts
EMBEDDING_MODEL=text-embedding-3-small

# -----------------------------------------------------------------------------
# Voice Configuration
# -----------------------------------------------------------------------------
# OpenAI TTS voices: alloy, echo, fable, onyx, nova, shimmer
QUANT_VOICE=echo
HUSTLER_VOICE=fable

# -----------------------------------------------------------------------------
# arXiv Configuration
# -----------------------------------------------------------------------------
# Comma-separated arXiv category codes
# ARXIV_CATEGORIES=["cs.CR", "q-fin.TR", "cs.AI", "cs.LG"]
# ARXIV_MAX_RESULTS=10
# ARXIV_LOOKBACK_DAYS=365

# -----------------------------------------------------------------------------
# Convergence Engine
# -----------------------------------------------------------------------------
# CONVERGENCE_ENABLED=true
# CONVERGENCE_TOP_N_NEWS=5
# CONVERGENCE_CATEGORIES_PER_NEWS=3
# CONVERGENCE_MIN_SIMILARITY=0.35
# CONVERGENCE_MIN_RELEVANCE=0.4
# CONVERGENCE_WEIGHT=0.6

# -----------------------------------------------------------------------------
# Podcast Settings
# -----------------------------------------------------------------------------
# PODCAST_NAME=The Agentic Ledger
# TARGET_DURATION_SECONDS=298
# TARGET_WORD_COUNT=745
# MAX_DURATION_SECONDS=300

# -----------------------------------------------------------------------------
# Paths (override for Docker or custom layouts)
# -----------------------------------------------------------------------------
# OUTPUT_DIR=output
# ASSETS_DIR=assets
# TEMP_DIR=temp

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
# GCP_PROJECT_ID=         # Optional: Enable Google Cloud Logging

# -----------------------------------------------------------------------------
# YouTube (optional, for future upload feature)
# -----------------------------------------------------------------------------
# YOUTUBE_API_KEY=
# YOUTUBE_CLIENT_SECRETS_PATH=
# YOUTUBE_CREDENTIALS_PATH=
